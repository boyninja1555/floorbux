{
  "scripts": {
    "devbuild": "bun build --watch src/index.ts --outfile index.js --target browser",
    "devserve": "bunx serve .",
    "dev": "concurrently \"bun devbuild\" \"bun devserve\""
  }
}
